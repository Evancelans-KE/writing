const myArray = newFunction()

function newFunction() {
    return [321100, true, Monday, {}];
}
console.log(myArray)

 

const fruits = ['orange', 'guacamole','onion', 'chilly', 'banana']
const len = fruits.len
fruits[len -1];
fruits[len-3];

